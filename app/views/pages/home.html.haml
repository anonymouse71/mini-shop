.container.main-container.headerOffset.globalPaddingBottom
  / Main component call to action
  .col-lg-12.text-center
    .home-intro
      %h2
        Discover a Better Shopping
        %span Experience
        \-  clothes, fashion  more
    %hr
    %div{:style => 'clear:both'}
    = render :partial => 'home_slider'
    = render :partial => 'new_arrivals'

  = render :partial => 'featured_products'
  -#= render :partial => 'brands'


:javascript
   var mySwiper = new Swiper('.swiper-container',{
        pagination: '.box-pagination',
        keyboardControl: true,
        paginationClickable: true,
        slidesPerView: 'auto',
        autoResize:true,
        resizeReInit:true,
    });

    $('.prevControl').on('click', function(e){
        e.preventDefault()
        mySwiper.swipePrev()
    });
    $('.nextControl').on('click', function(e){
        e.preventDefault()
        mySwiper.swipeNext()
    });